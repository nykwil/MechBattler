{
  "name": "@mechbattler/sim",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "build": "tsc -p tsconfig.json",
    "balance": "node --import tsx scripts/balance.ts",
    "demo": "node --import tsx scripts/tank-vs-sniper.ts",
    "adapt": "node --import tsx scripts/adaptation.ts",
    "hitrate": "node --import tsx scripts/hitrate.ts",
    "diversity": "node --import tsx scripts/diversity.ts"
  },
  "devDependencies": {
    "tsx": "^4.23.1",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
