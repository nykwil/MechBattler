node_modules/
dist/
.DS_Store
*.log
.vite/
packages/game/artifacts/
*:Zone.Identifier

# graphify: generated knowledge graph (3.7MB, regenerated by `graphify update .`)
# and the machine-local skill/CLI integration. The hooks live in the untracked
# .claude/settings.local.json, not in the shared settings.json.
graphify-out/
.claude/skills/graphify/
*.graphify-bak

# Balance harness raw output: regenerable in ~4 min by `npm run balance:collect`,
# and it churns a timestamp on every run. The two files that ARE tracked are
# artifacts/balance-baseline.json (the deliberate reference point) and
# artifacts/balance-report.md (the diff that makes a swing visible in review).
artifacts/balance.json
artifacts/diversity.json
